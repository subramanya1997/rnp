blank_issues_enabled: true
contact_links:
  - name: Upstream NumPy (the oracle)
    url: https://github.com/numpy/numpy
    about: Behavior questions about NumPy itself belong upstream. rnp treats NumPy 2.5.2 as ground truth.
